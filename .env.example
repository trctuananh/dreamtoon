# Resend API Key for email notifications (get one at resend.com)
RESEND_API_KEY=
# Optional: The email address to send from (must be verified in Resend)
# Default is 'onboarding@resend.dev' for testing
RESEND_FROM_EMAIL=
